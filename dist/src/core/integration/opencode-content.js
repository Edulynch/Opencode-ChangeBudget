import { INSTRUCTIONS_MARKER, INSTRUCTIONS_PROFILE_METADATA, WRAPPER_MARKER, } from './opencode-types.js';
/** Generate the exact content of the plugin wrapper file. */
export function generateWrapperContent(fileUrl) {
    return `${WRAPPER_MARKER}\nexport { default } from "${fileUrl}";\n`;
}
/** Generate the exact content of the agent instructions file. */
export function generateInstructionsContent() {
    return `${INSTRUCTIONS_MARKER}\n${INSTRUCTIONS_PROFILE_METADATA} opencode -->\n# ChangeBudget workflow instructions\n\nChangeBudget is the scope authority for the active implementation task.\n\n## Before implementation\n\n- Check the repository lifecycle and whether a ChangeBudget contract is active: run \`changebudget status\`.\n- If the status reports \`uninitialized\`, run \`changebudget init\`, then run \`changebudget status\` again before starting a contract.\n- If the repository is initialized or closed and no contract is active, start one before modifying files: run \`changebudget start\` with the appropriate allowed paths and budget.\n- For a Spec-Kit \`Txxx\` task with no explicitly chosen budget, run \`changebudget diagnose Txxx\` to get a deterministic budget recommendation.\n- Never amend or widen a ChangeBudget contract automatically. For an unchanged-scope numeric budget shortfall only, request explicit developer authorization before running \`changebudget amend\`; otherwise ask the developer for a new or revised contract.\n- Never use \`changebudget amend\` for semantic scope, paths, permissions, dependencies, migrations, configuration, public API, stack policy, baseline evidence, or lifecycle state.\n\n## During implementation\n\n- Stay within the allowed paths declared in the active contract.\n- Do not modify \`.changebudget/**\` files manually.\n- Do not modify denied or protected paths.\n\n## After implementation\n\n- Run targeted validation (build, typecheck, focused tests).\n- Run \`changebudget check\` to verify the implementation stays within the contract.\n- If the result is \`REPAIR\`, bring the changes back within the existing contract. Do not amend or widen the contract automatically.\n- If the result is \`PASS\`, the implementation satisfies the contract. Note: \`PASS\` does not automatically mean a Spec-Kit task is complete.\n- Close the contract only after implementation validation succeeds: run \`changebudget close\`.\n`;
}
//# sourceMappingURL=opencode-content.js.map